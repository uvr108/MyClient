export class Campos {
    presupuestos: {
    id: string;
    name: string;
    cratedAt: any;
    updatedAt: any;
  };
}

export const CAMPOS: {} = {
  presupuestos : ['id', 'name', 'createdAt' ],
  items : ['id', 'name', 'monto', 'createdAt']
};
